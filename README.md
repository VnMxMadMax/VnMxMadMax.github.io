# Hammadur Rahman — Portfolio

Live at https://vnmxmadmax.github.io

Single-file static site (HTML/CSS/JS, no build step). The hero is an interactive vector field: the cursor acts as a query vector and the nearest "embeddings" light up, like top-k retrieval in RAG.
